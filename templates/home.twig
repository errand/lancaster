{% extends "base.twig" %}

{% block content %}

	{% for post in posts [:2] %}
		{{ post.link }}
		{{ post.title }}
		{{ post.post_date | date( 'd.m.Y H:i' ) }}
		{{ post.thumbnail.src }}
	{% endfor %}

	{% for post in posts [2:] %}
		{% include ('tease-post.twig') %}
	{% endfor %}

{% endblock %}
