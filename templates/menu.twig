<ul class="list">
	{% for item in items %}
		<li class="item">
	        <a class="nav-link"{% if item.target %} target="{{ item.target }}"{% endif%} href="{{ item.link }}">{{ item.title }}</a>
	    </li>
	{% endfor %}
</ul>
